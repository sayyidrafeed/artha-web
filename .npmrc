# Vercel deployment configuration
# Ensures npm is used for production builds
engine-strict=true
save-exact=false

# Allow peer dependency overrides for compatibility
legacy-peer-deps=false
force=false

# Package manager preference for Vercel
package-manager=npm
